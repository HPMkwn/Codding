#include <bits/stdc++.h>
using namespace std;

string longestNiceSubstring(string s) {

    vector<int> small(26);
    vector<int> large(26);
    int i=0;
    while (i<s.length())
    {
        // add to data structure 


        //check whether it is nice string or not
        // if it is nice string than add save index and length it this length is higher than available already
        // if not than do nothing
    }
    
}

int main() { return 0; }